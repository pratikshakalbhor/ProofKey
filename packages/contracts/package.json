{
  "name": "@verishield/contracts",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "compile": "compact compile src/credential-registry.compact managed/credential-registry",
    "compile:skip-zk": "compact compile --skip-zk src/credential-registry.compact managed/skip",
    "clean": "rm -rf managed",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@midnight-ntwrk/compact-runtime": "0.19.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
