{
  "buildCommand": "pnpm --filter web build",
  "outputDirectory": "packages/web/dist",
  "installCommand": "npm install -g pnpm@12.4.2 && pnpm install --frozen-lockfile",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}